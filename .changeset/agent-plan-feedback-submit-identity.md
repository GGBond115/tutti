---
"@tutti-os/agent-activity-core": patch
"@tutti-os/agent-gui": patch
---

Keep AgentGUI Prompt submit identities as opaque UUID v4 values and track Plan feedback through explicit pending-submit source metadata instead of encoded ID prefixes.
